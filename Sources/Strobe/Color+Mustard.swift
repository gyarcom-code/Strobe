import SwiftUI

extension Color {
    /// Brand accent — `#E1AD01` (mustard). A fixed value, independent of the
    /// system light/dark appearance.
    static let mustard = Color(red: 0xE1 / 255, green: 0xAD / 255, blue: 0x01 / 255)
}
